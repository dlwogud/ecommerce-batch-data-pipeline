name: ecommerce_batch_pipeline
version: "1.0.0"
config-version: 2

profile: ecommerce_batch_pipeline

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - target
  - dbt_packages

models:
  ecommerce_batch_pipeline:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
